# adding-numbers-api

## Description
Spring Boot application generated by Spring Boot Integration Agent.

## Project Structure
```
adding-numbers-api/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/example/addingnumbers/
│   │   │       ├── Application.java
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       └── model/
│   │   └── resources/
│   │       ├── application.properties
│   │       ├── static/
│   │       └── templates/
│   └── test/
│       └── java/
├── pom.xml
└── README.md
```

## How to Run

### Prerequisites
- Java 17 or higher
- Maven 3.6+

### Build the project
```bash
mvn clean install
```

### Run the application
```bash
mvn spring-boot:run
```

Or run the JAR file:
```bash
java -jar target/adding-numbers-api-0.0.1-SNAPSHOT.jar
```

## API Endpoints

The application runs on `http://localhost:8080`

### Available Endpoints:
- **GET** `/api/add`
- **POST** `/api/add/list`
- **POST** `/api/add/batch`
- **GET** `/api/add/range`
- **POST** `/api/add/weighted`

## Testing

Run tests with:
```bash
mvn test
```

## Technology Stack
- Java 17
- Spring Boot 3.2.5
- Maven
