
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adding-numbers-api-deployment
  labels:
    app: adding-numbers-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: adding-numbers-api
  template:
    metadata:
      labels:
        app: adding-numbers-api
    spec:
      containers:
      - name: adding-numbers-api
        image: <YOUR_ECR_REGISTRY>/adding-numbers-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "production"
        - name: JAVA_OPTS
          value: "-Xms512m -Xmx1024m"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
      restartPolicy: Always
